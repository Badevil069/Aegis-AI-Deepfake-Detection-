import { FileText } from 'lucide-react';

export const documentModeOptions = [
  { id: 'document', label: 'Document Verification', icon: FileText, accept: '.pdf,.doc,.docx,.xls,.xlsx,.csv,.zip,.png,.tif,.tiff', desc: 'PDF, DOC/DOCX, XLS/XLSX, CSV, ZIP, PNG, TIFF' },
];